import React from 'react';
import * as S from "./styles"

const Button: React.FC = () => {
  return(
    <S.ContainerButton>
        <span>Ver Mais</span>
    </S.ContainerButton>
  );
}

export default Button;